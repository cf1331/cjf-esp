#include "cjf/atomic_gpio.h"

// All atomic GPIO functionality is now template-based and header-only.
// This file is kept for potential future non-template implementations
// or legacy compatibility functions if needed.

namespace cjf::atomic_gpio
{
  // Currently all functions are template-based and defined in the header
  // for optimal performance and zero-overhead abstractions.
}