#pragma once
// Host stub: empty sdkconfig.h to satisfy transitive includes from ESP-IDF
// headers (esp_compiler.h -> sdkconfig.h). All CONFIG_* macros are simply
// undefined, causing guarded blocks to be skipped.
